version: "3.9"

services:
  sproutops:
    image: nginx:alpine
    container_name: sproutops
    ports:
      - "8333:80"
    volumes:
      - ./app:/usr/share/nginx/html:ro
    restart: unless-stopped
